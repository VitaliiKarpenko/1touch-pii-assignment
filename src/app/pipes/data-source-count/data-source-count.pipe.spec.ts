import { TestBed } from '@angular/core/testing';
import { DataSourceCountPipe } from './data-source-count.pipe';

describe('Pipe: DataSourceCounte', () => {
  it('create an instance', () => {
    let pipe = new DataSourceCountPipe();
    expect(pipe).toBeTruthy();
  });
});
