import { AllPiiTypesPipe } from './all-pii-types.pipe';

describe('Pipe: AllPiiTypese', () => {
  it('create an instance', () => {
    let pipe = new AllPiiTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
