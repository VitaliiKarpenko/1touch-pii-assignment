import { TestBed } from '@angular/core/testing';
import { PiiTypesPipe } from './pii-types.pipe';

describe('Pipe: PiiTypese', () => {
  it('create an instance', () => {
    let pipe = new PiiTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
